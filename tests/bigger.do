type a := string[][]

proc print(s : string);

fun aux(i : string) : string with notEmpty =>  i

proc main(i : i32, argv : a) : { I : i32, b : i32[], cdefg: i32[][] } with NotZero
{
    let a := 5 + 3 * 2 + (1 + 2 ? 3 : 4);
    a[5];
    if (i = 0)
    {
        {
            let shortlived := 0;
        };
        a := 67 + 2 * a.b[5].c;
        return { I : 32, b : [1, 3], cdefg : [5;[[1,2,3,4], [5, 6]]]  };
    }
    else
    {

        fun a(a : string) : bool
        {
          return a != "";
        };

        if (a("2"))
        {
        };
        print("67 !");
    };
    return 85;
}

